import React from 'react';
import { Text, View } from 'react-native';

const MedicationNotes = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Medications notes</Text>
    </View>
  );
};

export default MedicationNotes;
